'use strict';

describe('myApp.damages module', function() {

  beforeEach(module('myApp.damages'));

  describe('damages controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var damagesCtrl = $controller('DamagesCtrl');
      expect(damagesCtrl).toBeDefined();
    }));

  });
});