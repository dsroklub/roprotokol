'use strict';

describe('myApp.rowers module', function() {

  beforeEach(module('myApp.rowers'));

  describe('rowers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rowersCtrl = $controller('RowersCtrl');
      expect(rowersCtrl).toBeDefined();
    }));

  });
});